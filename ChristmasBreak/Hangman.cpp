#include <iostream>
#include "Games.h"
#include <cstdlib>
#include <string>

void Hangman(int Category, int Difficulty)
{
	string FinalWord = Randomization(Category, Difficulty);
	int offset;	
	//I have to make it look through every character and switch it to a _, if it is a space then leave it be.
	FinalWord.replace

	//do
	//{

	//} while ();
}